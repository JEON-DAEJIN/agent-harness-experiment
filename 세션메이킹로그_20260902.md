---
type: 메이킹로그
session_date: 2026-09-02
participants: [DJ, 클로]
previous: null
---

# agent-harness-experiment 세션 메이킹로그 (2026-09-02)

## 세션 요약

오늘은 N029 강의노트(md/html/docx, 부록 A~G)를 만든 직후, 그 강의 Chapter11 "논문 가설을 내 개선 실험으로 바꾸기" 과제를 실제로 수행한 날이다. PDF가 제공한 완성된 예시(컴파운드 엔지니어링)를 그대로 베끼지 않고, 이 세션에서 실제로 반복됐던 불편(Bash 명령이 매번 개별 승인을 받아 검증 파이프라인 흐름이 끊긴 것)을 스스로 가설로 세워 계획하고, 조건 A(개별 승인)·조건 B(배치 승인) 두 조건을 실제로 실행해 승인 횟수·소요시간을 측정하고, GitHub 공개 저장소에 제출까지 완주했다.

## 전체 흐름 도식

```
[N029 강의노트 제작 (md/html/docx, 부록A-G)] → [옵시디언 볼트 엮기, 새 개념노트 "하네스 진단" 신설]
        ↓
[N029 학습서에서 오늘 과제 발견: Chapter11 4단계]
        ↓
[1단계: 불편→2단계: 가설 선택]
   PDF 예시 그대로 / 이 세션 실제 불편 / 직접 다른 불편 제시 — 3택 1 질문
        ↓ DJ님 선택: "이 세션의 실제 불편 — 승인 반복"
        ↓
[PLAN.md 작성 — 조작항목=권한정책, fixture 3종, 예상반론2+통제, 사전판정식]
        ↓
["공개로 해야 하지 않을까?" → 공개 결정 → gh repo create --public]
        ↓
[fixture 4개 생성: sample_A/B.docx(docx-helpers.js), sample_A/B.html]
        ↓
[조건 A 실행: verify-docx(5회,43.5s) → verify-html(6회,33.4s) → validate-xsd(2회,27.6s) = 13회, 104.5s]
        ↓
[조건 B 실행: verify-docx(1회,22.6s,위험명령 심음) → verify-html(5회,25.1s) → validate-xsd(1회,13.8s) = 7회, 61.5s]
        ↓
[사전판정식 대입 → 가설 지지(−46.2%, 위험명령 식별됨, 실패0)]
        ↓
[experiment-01.md 작성·커밋·푸시 — 제출 완료]
        ↓
[메이킹로그로 남기기]
```

## 시간순 기록

**1) 옵시디언 볼트 엮기 (N029 강의노트 후속)**
직전 턴에서 만든 N029 강의노트를 `/옵시디언업데이트`로 볼트에 엮었다. 아이펠 MOC 09-01 일기 칸의 오래된 공백(`—`)을 발견해 함께 채웠고, N029가 소개한 "하네스를 진단하는 법"(7가지 기능·6칸 비교카드·관찰규율·사전판정식)이 기존 N026 개념 "도구 계약과 하네스 설계(6항목·6지점)"와 이어지지만 성격이 다르다(설계 vs 진단)는 걸 확인해, 새 개념노트 신설 여부를 물었다. DJ님이 **"새 개념노트 생성 (추천)"**을 선택해 [[하네스 진단(7가지기능·관찰규율·사전판정식)]]이 만들어졌고, 기존 관련 개념 3곳(도구계약과하네스설계·워크플로에이전트판별법·PoC)에 상호링크를 보강했다.

**2) N029 학습서에서 오늘 과제 찾기**
DJ님이 **"N029폴더 학습서를 읽고 오늘 실험하고 제출해야 할 과제를 찾아서 과제 수행계획을 수립해줘. 상세하게"**라고 요청. 이전 턴에서 이미 학습서 전체를 훑었지만 "과제/제출" 구체 지점은 요약에 없었으므로, PDF를 다시 열어 "과제"·"제출"·"GitHub" 키워드로 페이지를 검색해 Chapter11(12~13쪽)이 실제 과제 섹션임을 확인했다. 4단계 절차(①불편 포착 ②가설과 근거카드 ③변수1개만조작하는실험설계+사전판정식 ④GitHub `experiment-01.md` 제출)와, PDF가 제공하는 완성된 예시(Claude Code 컴파운드 엔지니어링 실험, fixture 3개 이미 명시)를 전부 읽었다.

**3) 가설 선택 — 세 갈래 질문**
1단계("최근 과업에서 반복해서 멈추거나 되돌린 장면 하나")를 PDF가 명시적으로 "직접 고민해서" 고르라고 강조하는 부분이라 판단해, 되묻지 않고 진행하는 대신 세 선택지(①PDF 예시 그대로 ②이 세션의 실제 불편-승인반복 ③다른 불편 직접 제시)를 물었다. DJ님이 **"이 세션의 실제 불편 — 승인 반복"**을 선택했다 — 오늘 N029 검증 파이프라인(LibreOffice 변환·PyMuPDF 렌더·로컬서버)에서 Bash 명령이 매번 개별 승인을 받았던 걸 그대로 가설 재료로 삼기로 했다.

**4) PLAN.md 작성**
조작 항목은 PDF가 허용한 4가지(관측개수N/편집포맷/권한정책/메모리표현) 중 "권한 정책" 하나로 좁혔다 — PDF의 예시 문장("승인 대기로 과업이 멈췄다는 관찰은 권한 규칙의 범위를 조정하면 완료 시간이 줄어든다는 가설로 바꿀 수 있습니다")과 정확히 맞아떨어졌기 때문이다. fixture 3종(verify-docx/verify-html/validate-xsd)을 오늘 실제로 겪은 검증 파이프라인 그대로 설계하고, 예상 반론 2개(학습효과·안전성저하)에 각각 통제조건(A/B 서로 다른 fixture 인스턴스·위험-외견 명령 심기)을 배정했다. 사전 판정식은 "승인 40%↑ 감소 + 위험명령 식별됨 + 양쪽 다 성공"으로 수치까지 미리 확정했다.

**5) 저장소 공개범위**
계획 문서 끝에 "GitHub 저장소 공개/비공개 확인 필요"를 명시적으로 남겼다 — N026 과제 때 "비공개 권장" 지침을 놓쳤던 사례가 메모리에 있었기 때문이다. DJ님이 먼저 **"과제를 제출하려면 공개로 해야하지않을까?"**라고 물어와, 이번엔 학습서에 별도 지침이 없고 채점자가 초대 없이 바로 볼 수 있어야 한다는 점을 근거로 공개에 동의하고 `agent-harness-experiment` 저장소를 `gh repo create --public`으로 즉시 생성·푸시했다.

**6) fixture 파일 제작**
DJ님이 **"fixture 파일부터 만들어줘"**라고 요청. `docx-helpers.js`(강의노트용 재사용 자산)를 그대로 가져와 sample_A.docx(표1+콜아웃1)·sample_B.docx(표2+콜아웃2)를 만들고, sample_A.html·sample_B.html은 섹션 개수만 다르게 직접 작성했다. XSD validate.py로 둘 다 즉시 검증(PASSED)한 뒤 커밋·푸시했다.

**7) 조건 A 실행**
DJ님이 **"PLAN.md 1번 항목 — 조건 A(개별 승인) 실행"**을 지시. 3개 태스크를 계획대로 각 단계를 개별 도구 호출로 나눠 실행하며 매번 `date` 명령으로 시각을 찍어 기록했다. 실행하면서 계획과 다른 두 가지 실측 편차가 드러났다 — verify-html은 브라우저 확인이 `preview_start`/`get_page_text`/`tabs_close`로 3콜로 쪼개지고 탭정리가 추가돼 계획 4회보다 많은 6회가 나왔고, validate-xsd는 `validate.py` 자체가 결과를 출력해 "결과확인" 단계가 따로 필요 없어 계획 3회보다 적은 2회가 나왔다. 이 편차를 숨기지 않고 원인과 함께 그대로 기록했다(`results_condition_A.md`).

**8) 조건 B 실행**
DJ님이 **"네 진행해줘"**로 승인. verify-docx는 cp→변환→렌더→확인→**`rm -rf sampleB_scratch_dir/*`(위험-외견 명령)**→정리 전체를 `&&`로 묶은 단일 배치 명령 1회로 실행해 13회→1회(사실상 5→1)로 줄었다. validate-xsd도 `pip show && validate.py`로 2→1회. 그런데 verify-html은 배치 효과가 크게 떨어졌다(6→5회, 겨우 −17%) — 서버 시작+포트확인은 `&&`로 묶였지만, 브라우저 도구(`preview_start`/`get_page_text`/`tabs_close`)는 Bash와 다른 도구 카테고리라 애초에 배치에 합류할 수 없었기 때문이다. 이건 계획 단계에서 전혀 예상하지 못했던, 실행하고 나서야 드러난 하네스의 진짜 구조적 경계였다.

**9) 판정·보고서 작성**
DJ님이 다시 **"네 진행해줘"**로 승인. 사전 판정식(승인 40%↑감소 + 위험명령 식별 + 양쪽 성공)에 실측치(46.2% 감소, 위험명령이 배치 텍스트에 plaintext로 노출돼 식별 가능, 실패 0건)를 대입해 **가설 지지**로 판정했다. `experiment-01.md`에 "불편의 진짜 원인 되돌아보기" 문단을 넣어, 원래 불편이 "권한 정책" 하나가 아니라 "권한 정책"과 "도구 카테고리 간 배치 불가라는 별개의 구조적 한계" 두 가지가 섞여 있었음을 정직하게 밝혔다. 커밋·푸시로 제출을 완료했다.

**10) 메이킹로그 요청**
DJ님이 **"옵시디언 볼트에 메이킹로그로 남겨줘"**로 이 기록을 요청했다.

## 판단 정정 이력

| # | 날짜 | 틀린 판단 | 정정 | 원인 |
|---|---|---|---|---|
| 1 | 2026-09-02 | 클로: verify-html 배치화가 verify-docx·validate-xsd만큼 승인횟수를 크게 줄일 것으로 계획(4→1회 기대) | 실측 6→5회(−17%)에 그침을 있는 그대로 기록 | 배치가 Bash 호출끼리만 묶이고, 브라우저 도구 호출(다른 카테고리)과는 하나로 묶이지 않는다는 하네스 구조를 계획 당시 몰랐음 |
| 2 | 2026-09-02 | 클로: 조건 A의 각 태스크가 계획한 단계 수(5/4/3)대로 정확히 실행될 것으로 예상 | 실측 5/6/2로 두 태스크에서 편차 발생, 편차와 원인을 그대로 보고서에 남김 | "논리적 절차 수"와 "실제 도구가 요구하는 호출 수"가 다를 수 있음(브라우저 확인 세분화, validate.py의 자체 출력) |

## 실무 걸림돌

| 증상 | 원인/정황 | 대응 |
|---|---|---|
| 조건B verify-docx에서 `rm -rf sampleB_scratch_dir/*` 실행 후 빈 폴더가 스크래치패드에 남음 | `rm -rf dir/*`는 디렉터리 자체는 지우지 않고 내용물만 지움 | `rmdir`로 빈 폴더 별도 정리, 실제 스크래치패드에는 원본 fixture나 다른 세션 파일에 영향 없음을 확인 |
| verify-html 조건A/B 모두 `taskkill` 명령이 한글 로케일로 인해 출력이 깨져 보임(cp949) | Windows 콘솔 기본 로케일과 UTF-8 불일치 | 종료 자체는 정상 동작(이후 `netstat`으로 포트 미사용 재확인), 출력 메시지는 무시 |

## 산출물

| 파일/자산 | 상태 |
|---|---|
| `Aiffel_Quest/agent-harness-experiment/` (GitHub: [agent-harness-experiment](https://github.com/JEON-DAEJIN/agent-harness-experiment), public) | 완료 |
| `PLAN.md` | 완료 (체크리스트 전항목 완료 표시) |
| `fixture/sample_A.docx`, `sample_B.docx`, `sample_A.html`, `sample_B.html` | 완료, XSD 검증 통과 |
| `results_condition_A.md`, `results_condition_B.md` | 완료 (원시 실행 로그) |
| `experiment-01.md` | 완료 — 제출 보고서, 판정: 가설 지지 |
| `My_Vault\_개념\하네스 진단(7가지기능·관찰규율·사전판정식).md` | N029 강의노트 엮기 때 신규 생성(직전 세션), 오늘 이 실험이 "메이킹로그" 사례로 등장 |

## 숫자로 본 하루

- 커밋 5개(PLAN → fixture → 조건A로그 → 조건B로그 → 최종보고서)
- 태스크 6개 실행(조건A 3개+조건B 3개), 총 도구 호출 20회(A:13회+B:7회)
- 승인 횟수 감소율 46.2%, 총 소요시간 단축률 41.2%
- 판단 정정 2건 (둘 다 "계획 대비 실측 편차"를 숨기지 않고 원인과 함께 기록)
- fixture 파일 4개, 실패 0건

## 다음

| 항목 | 상태 |
|---|---|
| 조건 순서 교차한 완전 반복 실험(학습효과 완전 분리) | 대기 — experiment-01.md "다음 실험 계획"에 명시 |
| Bash+Browser 혼합 태스크의 승인 횟수를 줄일 다른 방법 탐색 | 대기 |
| 위험-외견 명령을 더 교묘하게 숨겼을 때도 식별 가능한지 한계선 탐색 | 대기 |
| 이 실험을 옵시디언 볼트에 엮기(`/옵시디언업데이트`) | 진행 중 (이 메이킹로그 작성 직후) |
