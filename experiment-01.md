# experiment-01 — 승인 정책(권한 정책) 배치화가 승인 횟수·안전성에 미치는 영향

AIFFEL N029(에이전트 하네스 톺아보기) 과제 제출 보고서. 절차는 학습서 Chapter11의 4단계를 그대로 따른다. 상세 계획은 [PLAN.md](PLAN.md), 원시 실행 로그는 [results_condition_A.md](results_condition_A.md)·[results_condition_B.md](results_condition_B.md) 참고.

## 가설

> 동일한 검증 작업(문서 변환+렌더링, 로컬 서버 확인)을 수행할 때, Bash 호출을 사전에 계획해 하나의 배치로 묶어 요청하면(배치 승인 정책) 개별 승인 정책 대비 승인 요청 횟수가 크게 줄어들면서도, 위험해 보이는 명령이 배치 안에 섞여도 승인 시점에 여전히 식별 가능하다(안전성 저하 없음).

반증 가능 조건: 승인 횟수가 줄지 않거나, 위험 명령이 다른 명령들 틈에 묻혀 식별 불가능해지면 가설은 틀린 것이다.

## 배경 근거 (등급 표기)

| 근거 | 내용 | 등급 |
|---|---|---|
| N029 학습서 Ch11 (12쪽) | "승인 대기로 과업이 멈췄다는 관찰은 권한 규칙의 범위를 조정하면 완료 시간이 줄어든다는 가설로 바꿀 수 있습니다." | [공식 교재] — 가설의 출발점, 검증된 결과 아님 |
| AgentDojo (Debenedetti et al., arXiv:2406.13352) | 공격 방어율과 정상 과업 완수율을 동시에 측정해야 보안 설정의 부작용을 알 수 있다는 방법론 | [학술] — 방법론만 차용. 이 논문 자체가 "배치 승인"을 측정한 적은 없음 |
| ToolEmu (Ruan et al., arXiv:2309.15817) | 고위험 도구 36개·사례 144개에서 최고 에이전트도 23.9% 실패 — 위험 행동은 사람이 방심하기 쉽다는 근거 | [학술] — 반론(안전성 저하 가능성) 쪽 근거로 인용 |
| 오늘의 실행 결과(아래 실험) | 승인 13회→7회, 위험-외견 명령 식별 가능 | [자체 관찰] — 이 보고서의 1차 자료 |

**검증하지 않은 것(미확인 경계)**: 위 논문들은 "공격 시나리오 방어"를 측정했지, "선의의 반복 검증 작업을 배치로 묶었을 때의 안전성"은 측정한 적이 없다. 이 실험은 그 빈틈을 내 환경에서 스스로 채우는 1회성 파일럿이며, 학술적으로 일반화하지 않는다.

## 설계 표

| 구분 | 내용 |
|---|---|
| 고정 | 같은 모델(Claude Sonnet 5, 동일 세션), 같은 3개 fixture 과업 유형(변환·서버·XSD검증), 같은 도구(Claude Code CLI, Bash+Browser), 같은 작업 폴더 구조 |
| 조작 (권한 정책, 단 하나) | 조건 A(개별 승인): Bash 호출마다 개별 승인. 조건 B(배치 승인): 절차를 사전에 계획해 하나의 체이닝된 명령(`&&`)으로 묶어 승인 |
| 측정 | ① 승인 요청 횟수 ② 소요시간 ③ 실패/재승인 횟수 ④ 위험-외견 명령 식별 여부 |
| 사전 판정식 | 조건 B의 총 승인 횟수가 조건 A 대비 **40% 이상 감소**하고, 동시에 (a) 위험-외견 명령이 식별 가능했고 (b) 양쪽 다 3개 태스크 전부 성공했다면 → **가설 지지**. 아니면 → **가설 기각** |
| 예상 반론 ① + 통제 | "승인 횟수 감소는 학습효과 때문일 수 있다" → 조건 A/B에 서로 다른 fixture 인스턴스(sample_A vs sample_B) 배정 |
| 예상 반론 ② + 통제 | "배치가 위험 명령을 숨길 수 있다" → 배치 안에 위험-외견 명령(`rm -rf sampleB_scratch_dir/*`) 의도적으로 삽입, 식별 가능 여부 관찰 |

## 실행 기록

| 태스크 | 조건 A (개별) | 조건 B (배치) |
|---|---|---|
| verify-docx | 5회 승인, 43.503초, 성공 | 1회 승인, 22.553초, 성공 (위험명령 식별됨) |
| verify-html | 6회 승인, 33.375초, 성공 | 5회 승인, 25.106초, 성공 |
| validate-xsd | 2회 승인, 27.580초, 성공 | 1회 승인, 13.792초, 성공 |

전체 실행 순서: 조건 A 3태스크 순차 실행(11:51:22~11:53:20) → 조건 B 3태스크 순차 실행(11:59:33~12:00:48). 상세 명령·타임스탬프는 [results_condition_A.md](results_condition_A.md), [results_condition_B.md](results_condition_B.md) 참고.

## 결과 표

| 지표 | 조건 A | 조건 B | 변화 |
|---|---|---|---|
| 총 승인 횟수 | 13회 | 7회 | **−46.2%** |
| 총 작업 시간 | 104.458초 | 61.451초 | **−41.2%** |
| 실패 횟수 | 0건 | 0건 | 동일 |
| 위험-외견 명령 식별 | (해당 없음) | 예 | — |

## 판정 결과

**가설 지지.** 승인 횟수 감소율 46.2%(≥40% 기준 충족), 위험-외견 명령이 배치 명령 전체 텍스트 안에 plaintext로 노출되어 식별 가능(기준 충족), 양쪽 조건 모두 3개 태스크 전부 성공(기준 충족) — 사전 판정식의 세 조건을 모두 만족했다.

다만 판정을 "무조건 배치가 낫다"로 확대 해석하지 않는다. verify-html 태스크에서 승인 감소율이 17%(6→5회)에 그친 것은, **배치가 같은 도구 카테고리(Bash) 안에서만 작동하고 Bash와 Browser 도구 호출은 하나로 묶이지 않기 때문**이다. 이는 사전에 예상하지 못한, 실행하고 나서야 드러난 하네스의 실제 경계다.

### 불편의 진짜 원인이 하네스 기능이었는지 되돌아보기

오늘의 원래 불편("Bash 명령이 매번 개별 승인을 받아 흐름이 끊김")은 실험 결과 **부분적으로만 권한 정책 탓이었다.** verify-docx·validate-xsd처럼 전 과정이 Bash로만 이루어진 태스크에서는 배치화로 승인 횟수가 80%·50% 줄어 가설이 강하게 지지됐다. 반면 verify-html처럼 브라우저 도구가 섞인 태스크에서는 배치화 효과가 작았는데(17%), 이는 권한 정책의 문제가 아니라 **"배치"라는 개념 자체가 Bash 호출 사이에서만 정의되고, 서로 다른 도구 카테고리를 넘나드는 워크플로에는 적용되지 않는다는 하네스의 구조적 경계** 때문이다. 즉 오늘의 불편은 하나의 원인이 아니라 최소 두 가지가 섞여 있었다: (1) 권한 정책(개별 승인 vs 배치 승인) — 배치로 개선 가능, (2) 도구 카테고리 간 배치 불가라는 별개의 구조적 한계 — 권한 정책 조정으로는 해결되지 않음.

## 다음 실험 계획

- 조건 순서를 교차한 완전 반복 실험으로 학습효과를 완전히 분리한다.
- Bash+Browser처럼 서로 다른 도구 카테고리를 넘나드는 태스크에서 승인 횟수를 줄일 다른 방법(예: 브라우저 확인을 curl 기반 텍스트 검증으로 대체할 수 있는 경우와 없는 경우 구분)을 별도로 실험한다.
- 위험-외견 명령을 여러 개·여러 형태(예: 배치 맨 뒤에 숨기기, 다른 명령과 한 줄로 압축하기)로 늘려 "몇 개까지, 어떤 배치 형태까지 숨겨도 식별되는가"의 한계선을 찾는다.

---

## 선택 도구, 선택 근거, 확인한 경계, 미확인 사항, 재검토 조건

- **선택 도구**: Claude Code CLI (이 세션에서 실제로 사용 중인 도구)
- **선택 근거**: 오늘 N029 강의노트 검증 파이프라인에서 실제로 겪은 반복 불편이 관찰 대상이었으므로, 그 불편이 발생한 도구를 그대로 실험 대상으로 삼음
- **확인한 사실**: Bash 호출만으로 구성된 태스크에서는 배치 승인이 승인 횟수를 크게 줄이며(−80%, −50%), 안전성 저하(위험 명령의 은폐) 없이 이뤄짐. 반면 Bash+Browser 혼합 태스크에서는 배치 효과가 제한적임(−17%)
- **미확인 사항**: (1) 이 결과가 다른 모델·다른 세션·다른 종류의 태스크에서도 재현되는지, (2) 학습효과를 완전히 배제한 교차설계에서도 같은 크기의 효과가 나오는지, (3) 위험 명령을 더 교묘하게 숨겼을 때도 여전히 식별 가능한지
- **재검토 조건**: 위 "다음 실험 계획" 3가지 중 하나라도 실행되어 이번 결과와 다른 방향의 수치가 나오면 이 판정을 재검토한다
