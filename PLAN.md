# N029 과제 수행계획 — 승인 정책(권한 정책) 배치화 실험

> 근거: `강의정리/N029/N029_에이전트_하네스_톺아보기(agent-harness)_학습서.pdf` Chapter11 "논문 가설을 내 개선 실험으로 바꾸기" (PDF 12~13쪽)의 4단계 절차를 그대로 따른다.
> 제출 형식(학습서 원문 그대로): GitHub 저장소에 `experiment-01.md`로 제출. 목차 — 가설 → 배경 근거(등급 표기) → 설계 표 → 실행 기록 → 결과 표 → 판정 결과 → 다음 실험 계획.

---

## 0. 왜 이 불편을 골랐는가

학습서는 "PDF 예시를 그대로 베끼지 말고 직접 겪은 불편에서 출발하라"고 명시한다. 오늘 N029 강의노트(md/html/docx 3종)를 만들고 검증하는 동안 실제로 반복된 장면을 골랐다:

> **관찰한 장면**: docx 검증 파이프라인(스크래치패드로 복사 → LibreOffice로 PDF 변환 → PyMuPDF로 페이지 렌더 → 결과 확인 → 임시파일 정리)과 html 검증 파이프라인(로컬 서버 실행 → 브라우저로 확인 → 서버 종료)에서, Bash 명령이 매번 개별적으로 승인 요청을 받아 실행 흐름이 자주 끊겼다.

이 불편이 정말 "하네스 기능"에서 온 것인지 먼저 의심해본다(학습서가 요구하는 자기 점검):
- 모델 선택 탓? — 아니다. 같은 세션·같은 모델에서 반복됐다.
- 과업 난이도 탓? — 아니다. 파일 변환·서버 실행은 매번 거의 동일한 정형 작업이었다.
- 저장소/네트워크 상태 탓? — 아니다. 로컬 파일·로컬 서버로 외부 의존성이 없었다.
- 요청 문장 탓(내가 명령을 잘게 쪼개 요청해서)? — **부분적으로 그렇다.** 하지만 명령을 한 번에 묶어 요청할지 여부는 결국 "승인이 어느 단위로 요구되는가"라는 권한 정책의 문제로 되돌아온다.

→ 남는 후보는 **권한과 보안(Chapter08) 기능**, 그중에서도 **승인이 요구되는 단위(정책)**다.

---

## 1단계 — 불편 → 가설로 바꾸기 (완료)

학습서 예시 문장을 그대로 내 상황에 적용하면:

> "승인 대기로 과업이 멈췄다는 관찰은 권한 규칙의 범위를 조정하면 완료 시간이 줄어든다는 가설로 바꿀 수 있습니다." (학습서 12쪽)

## 2단계 — 가설과 근거 카드

### 가설 (반증 가능한 형태)

> **동일한 검증 작업(문서 변환+렌더링, 로컬 서버 확인)을 수행할 때, Bash 호출을 사전에 계획해 하나의 배치로 묶어 요청하면(배치 승인 정책) 개별 승인 정책 대비 승인 요청 횟수가 크게 줄어들면서도, 위험해 보이는 명령이 배치 안에 섞여도 승인 시점에 여전히 식별 가능하다(안전성 저하 없음).**

반대로 나오면(승인 횟수가 안 줄거나, 위험 명령이 다른 명령들 틈에 묻혀 식별 안 되면) 가설은 틀린 것이다 — 반증 가능.

### 배경 근거 카드 (등급 표기)

| 근거 | 내용 | 등급 |
|---|---|---|
| N029 학습서 Ch11 (12쪽) | "승인 대기로 과업이 멈췄다는 관찰은 권한 규칙의 범위를 조정하면 완료 시간이 줄어든다는 가설로 바꿀 수 있습니다." | [공식 교재] — 가설의 출발점, 검증된 결과 아님 |
| AgentDojo (Debenedetti et al., arXiv:2406.13352) | 공격 방어율과 **정상 과업 완수율**을 동시에 측정해야 보안 설정의 부작용(정상 업무 방해)을 알 수 있다는 방법론 | [학술] — 단, 이 논문은 "공격 시나리오"를 다루지, "본인이 짠 정상 명령을 배치로 묶는 상황"을 측정한 적은 없다. **여기서는 방법론만 차용**(정상 완수율/부작용을 같이 재라는 원칙) |
| ToolEmu (Ruan et al., arXiv:2309.15817) | 고위험 도구 36개·사례 144개에서 최고 에이전트도 23.9% 실패 — 위험 행동은 사람이 방심하기 쉽다는 근거 | [학술] — 배치화가 위험 명령을 "숨길" 수 있다는 반론(아래)의 근거로 인용, 가설을 지지하는 근거가 아니라 **반론 쪽 근거**로 정직하게 씀 |
| 오늘의 관찰(N029 검증 세션) | 위 0번 항목 | [자체 관찰] — 이번 실험의 유일한 1차 자료. 논문 수치가 아니라 오늘 직접 겪은 불편 |

> **검증하지 않은 것(미확인 경계)**: 위 논문들은 "공격 시나리오에서의 방어"를 측정했지, "선의의 반복 검증 작업을 배치로 묶었을 때의 안전성"은 측정한 적이 없다. 이 실험은 그 빈틈을 내 환경에서 스스로 채우는 파일럿이다 — 학술적으로 일반화하지 않는다.

---

## 3단계 — 실험 설계 (변수 1개만 조작)

### 조작 항목: **권한 정책** (학습서가 허용한 4가지 조작 항목 — 관측 개수 N / 편집 포맷 / 권한 정책 / 메모리 표현 — 중 하나)

| 구분 | 내용 |
|---|---|
| **고정** | 같은 모델(Claude Sonnet 5, 이 세션), 같은 3개 fixture 과업 **유형**(변환·서버·XSD검증), 같은 도구(Claude Code CLI, Bash), 같은 작업 폴더 구조(`agent-harness-experiment/fixture/`), 시간·토큰 예산 상한 없음(대신 실제 소요시간을 측정치로 기록) |
| **조작** (권한 정책, 단 하나만) | **조건 A(개별 승인)**: 지금까지 해온 기본 방식 그대로 — Bash 호출마다 개별적으로 승인받는다. vs **조건 B(배치 승인)**: 각 태스크의 전체 절차를 미리 계획해 하나의 체이닝된 명령(`&&`)이나 한 개 셸 스크립트로 묶어 한 번만 승인받는다. |
| **측정** (이중 이상 측정) | ① 승인 요청 횟수(핵심 종속변수) ② 태스크 시작~완료 소요시간 ③ 실패/재승인 횟수 ④ **위험-외견 명령 식별 여부**(안전성 지표, 아래 통제조건 참고) |
| **사전 판정식** | 조건 B의 총 승인 횟수가 조건 A 대비 **40% 이상 감소**하고, 동시에 (a) 배치 안에 심어둔 위험-외견 명령이 승인 요청 화면에서 실제로 식별 가능했고 (b) 두 조건 모두 3개 태스크가 전부 성공 완료됐다면 → **가설 지지**. 승인 횟수 감소가 40% 미만이거나, 위험-외견 명령이 다른 명령들 틈에 묻혀 식별 불가능했거나, 어느 한 조건에서라도 태스크가 실패했다면 → **가설 기각**. |

### 예상 반론 2개 + 통제 조건

| 반론 | 통제 조건 |
|---|---|
| ① "승인 횟수 감소는 배치화 때문이 아니라, 두 번째로 실행할수록 명령어에 익숙해져(학습 효과) 명령 자체가 단순해졌기 때문일 수 있다" | 조건 A와 조건 B에 **서로 다른 fixture 파일 인스턴스**를 배정한다(구조는 동일, 구체적 내용만 다름 — 예: `sample_A.docx`는 표 1개+콜아웃 1개, `sample_B.docx`는 표 2개+콜아웃 2개). 완전한 교차설계(A·B 순서를 바꿔 2회 반복)까지는 이번 1차 파일럿에 넣지 않고, "다음 실험 계획"에 남긴다 — 이 잔여 한계는 보고서에 정직하게 기록한다. |
| ② "배치로 묶으면 편의성은 올라가도 위험한 명령이 안전한 명령들 틈에 숨어 심사가 느슨해질 수 있다" | 조건 B의 배치 3개 중 최소 1개에 **겉보기엔 위험해 보이지만 실제로는 스크래치패드 안이라 안전한 명령**(예: 스크래치패드 하위 폴더 대상 `rm` 패턴)을 의도적으로 섞어 넣고, 승인 프롬프트에 그 명령이 실제로 표시·식별되는지 관찰한다. |

### fixture 과업 3종 (조건 A/B 공통 절차, 구체 파일만 다름)

| 태스크 | 절차 (개별 승인 시 전형적 단계 수) | A용 fixture | B용 fixture |
|---|---|---|---|
| `verify-docx` | ①스크래치패드로 복사 ②LibreOffice로 PDF 변환 ③PyMuPDF로 2쪽 PNG 렌더 ④결과 확인(ls) ⑤테스트 산출물 정리(rm, 파일명 지정) — **5단계** | `fixture/sample_A.docx` (표 1개+콜아웃 1개, docx-helpers.js로 미리 생성) | `fixture/sample_B.docx` (표 2개+콜아웃 2개) |
| `verify-html` | ①로컬 서버 실행(`python -m http.server`) ②포트 확인 ③내용 확인(get_page_text 또는 curl) ④서버 프로세스 종료 — **4단계** | `fixture/sample_A.html` | `fixture/sample_B.html` |
| `validate-xsd` | ①의존성 확인(`pip show`) ②`validate.py` 실행 ③결과 확인 — **3단계** | `fixture/sample_A.docx` 재사용 | `fixture/sample_B.docx` 재사용 |

> 개별 승인 조건(A) 예상 총 승인 횟수: 5+4+3 = **12회**. 배치 승인 조건(B) 예상: 태스크당 1회 × 3 = **3회** (예상 75% 감소 — 실측은 다를 수 있음, 이게 실험 이유).

---

## 4단계 — 제출 계획

### 오늘 실행 체크리스트

- [ ] **0. 준비** — `agent-harness-experiment/fixture/`에 `sample_A.docx`, `sample_B.docx`, `sample_A.html`, `sample_B.html` 4개 더미 파일을 미리 만들어 고정한다(실행 중 내용을 바꾸지 않는다).
- [ ] **1. 조건 A 실행** — 위 3개 태스크를 지금까지 방식대로(개별 승인) 순서대로 실행하며, 승인 요청마다 시각·명령·승인여부를 표로 기록한다.
- [ ] **2. 조건 B 실행** — 동일 3개 태스크를 배치(체이닝/스크립트)로 묶어 실행하고, `verify-docx` 배치 안에 위험-외견 명령 1개를 심어 승인 시 식별 여부를 관찰·기록한다.
- [ ] **3. 결과표 작성** — 태스크별 승인횟수·소요시간·실패횟수·위험명령 식별여부를 표로 정리한다.
- [ ] **4. 사전 판정식 대입** — 지지/기각을 판정하고, 기각이어도 실패로 감추지 않고 원인 후보(하네스 기능 vs 다른 요인)를 되짚는 문단을 쓴다.
- [ ] **5. GitHub 저장소 생성 여부 확인** — ⚠️ 학습서는 이 과제의 저장소 공개범위(공개/비공개)를 명시하지 않았다. 만들기 전에 공개로 할지 비공개로 할지 확인이 필요하다(과거 N026 과제에서 "비공개 권장" 지침을 놓쳤던 사례가 있어 이번엔 미리 묻는다).
- [ ] **6. `experiment-01.md` 작성 및 커밋/푸시** — 가설·배경근거(등급표기)·설계표·실행기록·결과표·판정결과·다음 실험계획 순서로 작성. 마지막에 선택 도구(Claude Code)·선택 근거·확인한 경계·미확인 사항·재검토 조건을 남긴다.

### 저장소 제안

- 이름: `agent-harness-experiment` (이미 로컬에 폴더 생성해둠: `Aiffel_Quest/agent-harness-experiment/`)
- 공개범위: **확인 필요** (위 체크리스트 5번)

### 다음 실험 계획(보고서에 미리 적어둘 항목)

- 조건 순서를 교차한 완전 반복 실험(학습효과 완전 분리)
- 배치 승인 정책을 Claude Code의 실제 설정(permission mode)으로 재현해 다른 세션·다른 작업에서도 재현되는지 확인
- 위험-외견 명령을 여러 개·여러 형태로 늘려 "몇 개까지 숨겨도 식별되는가"의 한계선 찾기
